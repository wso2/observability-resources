class sample_bal_shipment::params inherits common::params {
  $samples_dir = "${deployment_dir}/bal_apps"
  $shipment_app_dir = "${samples_dir}/shipment"
  $shipment_app_logs_dir = "${shipment_app_dir}/logs"
}
