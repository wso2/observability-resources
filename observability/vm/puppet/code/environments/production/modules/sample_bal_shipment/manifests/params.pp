class sample_bal_shipment::params inherits common::params {
  $samples_dir = "${deployment_dir}/samples"
  $sales_app_dir = "${samples_dir}/shipment"
  $sales_app_logs_dir = "${sales_app_dir}/logs"
}
